{
  "private": true,
  "eslintConfig": {
    "extends": "@wildpeaks/commonjs",
    "rules": {
      "require-unicode-regexp": "off"
    }
  },
  "eslintIgnore": [
    "packages"
  ],
  "prettier": "@wildpeaks/prettier-config",
  "scripts": {
    "lint": "eslint src/**/*.js test/**/*.js",
    "build": "mocha src/build.js",
    "test": "mocha test/*.test.js"
  },
  "dependencies": {
    "mocha": "9.2.1",
    "rimraf": "3.0.2",
    "terser": "5.12.0"
  },
  "devDependencies": {
    "@wildpeaks/eslint-config-commonjs": "15.2.0",
    "@wildpeaks/prettier-config": "15.1.0",
    "eslint": "8.10.0",
    "jsdom": "19.0.0",
    "playwright": "1.19.2"
  }
}
