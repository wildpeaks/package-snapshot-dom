{
  "private": true,
  "eslintConfig": {
    "extends": "@wildpeaks/commonjs",
    "rules": {
      "require-unicode-regexp": "off"
    }
  },
  "eslintIgnore": [
    "packages"
  ],
  "prettier": "@wildpeaks/prettier-config",
  "scripts": {
    "lint": "eslint src/**/*.js test/**/*.js",
    "build": "mocha src/build.js",
    "test": "mocha test/*.test.js"
  },
  "dependencies": {
    "mocha": "9.2.2",
    "rimraf": "3.0.2",
    "terser": "5.12.1"
  },
  "devDependencies": {
    "@wildpeaks/eslint-config-commonjs": "15.3.0",
    "@wildpeaks/prettier-config": "15.3.0",
    "eslint": "8.12.0",
    "jsdom": "19.0.0",
    "playwright": "1.20.1"
  }
}
